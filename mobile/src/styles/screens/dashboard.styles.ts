import { StyleSheet } from 'react-native';

export const dashboardStyles = StyleSheet.create({
  content: {
    width: '100%',
    maxWidth: 600,
  },

  logo: {
    fontSize: 24,
    fontWeight: '900',
    marginBottom: 32,
  },

  title: {
    fontSize: 38,
    fontWeight: '800',
    marginBottom: 12,
  },

  subtitle: {
    fontSize: 17,
    lineHeight: 26,
    marginBottom: 32,
  },
});